# Локальные инструкции для Codex

- Полный набор тестов можно запускать через изолированное окружение проекта.
- При изменении клиентских diagnostic events, которые отправляются в `/api/v1/client/log`, обновляй `docs/client-diagnostics.md` в том же изменении.
