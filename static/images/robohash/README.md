These avatars are static SVG composites generated from vendored Robohash set1
PNG layers, sourced from `github.com/terem42/robohash` v1.0.7.

The chat client selects one of these files deterministically from the username,
so avatars do not depend on `robohash.org` or any runtime image generation.
